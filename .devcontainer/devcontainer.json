{
	"name": "AdamRMS",
	"dockerComposeFile": "docker-compose.yml",
	"service": "app",
	"workspaceFolder": "/workspace",

	// Configure tool-specific properties.
	"customizations": {
		// Configure properties specific to VS Code.
		"vscode": {		
			"settings": { 
				"php.validate.executablePath": "/usr/local/bin/php"
			},
			"extensions": [
				"xdebug.php-debug",
				"bmewburn.vscode-intelephense-client",
				"mrmlnc.vscode-apache"
			]
		}
	},

	"shutdownAction": "stopCompose",

	"forwardPorts": [8080, "db:3306"],

<<<<<<< Updated upstream
	"postCreateCommand": "composer install && php vendor/bin/phinx migrate && php vendor/bin/phinx seed:run",
=======
	"postCreateCommand": "composer install && php vendor/bin/phinx migrate",
>>>>>>> Stashed changes

	"postStartCommand": "php -S localhost:8080 -t html/admin/",

	"remoteUser": "vscode"
}
