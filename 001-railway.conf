<VirtualHost *:80>
	ServerName ${RAILWAY_STATIC_URL}
	DocumentRoot ${APACHE_DOCUMENTROOT}
	<Directory ${APACHE_DOCUMENTROOT}>
		Options -Indexes
		AllowOverride All
		Order allow,deny
		Allow from all
	</Directory>
</VirtualHost>