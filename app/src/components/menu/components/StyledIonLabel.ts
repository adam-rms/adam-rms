import styled from "styled-components";
import { IonLabel } from "@ionic/react";

const StyledIonLabel = styled(IonLabel)`
  ion-menu.md ion-item & {
    font-weight: 500;
  }
`;

export default StyledIonLabel;
